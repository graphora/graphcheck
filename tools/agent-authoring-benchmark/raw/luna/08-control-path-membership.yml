suite: 08-control-path-membership
generated: true
competency:
  - id: 08-control-path-membership
    question: Does CUST-RING-LEADER-0 reach ACC-2401 and ACC-2408 within four outgoing CONTROLS hops?
    params:
      customer_id: CUST-RING-LEADER-0
    query: |
      MATCH (c:Customer {id: $customer_id})-[:CONTROLS*1..4]->(a:Account)
      RETURN DISTINCT a.id AS account_id
    expect:
      columns:
        - account_id
      contains:
        - [ACC-2401]
        - [ACC-2408]
    severity: error
