suite: 08-control-path-membership
generated: true
competency:
  - id: 08-control-path-membership
    question: Does the ring leader control accounts ACC-2401 and ACC-2408 within four hops?
    query: |
      MATCH (c:Customer {id: $customer_id})-[:CONTROLS*1..4]->(a:Account)
      RETURN DISTINCT a.id AS account_id
    params:
      customer_id: CUST-RING-LEADER-0
    expect:
      contains:
        - account_id: ACC-2401
        - account_id: ACC-2408
    severity: error
