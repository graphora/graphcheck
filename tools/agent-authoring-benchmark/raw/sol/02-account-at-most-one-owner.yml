suite: 02-account-at-most-one-owner
generated: true
competency:
  - id: 02-account-at-most-one-owner
    question: Does every Account have at most one incoming OWNS relationship from a Customer?
    query: |
      MATCH (a:Account)
      OPTIONAL MATCH (c:Customer)-[ownership:OWNS]->(a)
      WITH a, count(ownership) AS owner_count
      WHERE owner_count > 1
      RETURN a.id AS account_id, owner_count
    expect:
      empty: true
    severity: error
